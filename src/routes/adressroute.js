// import express from 'express';
// import { requireApiLogin } from '../Middleware/userMiddleware.js';

// const router = express.Router();


// const handleAddressStorage = (req, res) => {
 

//   res.json({ success: true, message: 'Address received (placeholder)' });
// };


// router.post('/', requireApiLogin, handleAddressStorage);



// export default router;
